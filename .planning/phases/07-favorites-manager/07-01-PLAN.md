---
phase: 07-favorites-manager
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/desktop/AdminContext.tsx
  - components/desktop/DesktopShell.tsx
  - components/desktop/LoginModal.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Admin state true/false is available to any component inside DesktopShell via React context"
    - "Login with admin password ('jhered') sets admin state to true for the session"
    - "Login with non-admin password sets admin state to false"
    - "Admin state does not persist across page refresh (useState resets)"
    - "AdminProvider wraps all DesktopShell content including the login gate"
  artifacts:
    - path: "components/desktop/AdminContext.tsx"
      provides: "AdminProvider, useAdmin hook, ADMIN_SECRET constant"
      exports: ["AdminProvider", "useAdmin", "ADMIN_SECRET"]
    - path: "components/desktop/DesktopShell.tsx"
      provides: "DesktopShell with AdminProvider wrapping entire return"
      contains: "AdminProvider"
    - path: "components/desktop/LoginModal.tsx"
      provides: "LoginModal with admin password detection"
      contains: "setAdmin"
  key_links:
    - from: "LoginModal.handleSubmit"
      to: "AdminContext.setAdmin"
      via: "useAdmin() hook"
      pattern: "setAdmin(password === ADMIN_SECRET)"
    - from: "DesktopShell return"
      to: "AdminProvider"
      via: "JSX wrapping"
      pattern: "AdminProvider"
    - from: "AdminProvider"
      to: "LoginModal"
      via: "React context"
      pattern: "AdminProvider.*LoginModal"
---

<objective>
Create the cross-phase admin state bridge: an AdminContext that exposes `isAdmin`/`setAdmin` to the entire DesktopShell tree, wired into LoginModal for admin password detection.

**Purpose:** Admin state must be available to the Favorites program (Phase 7) and set by the Login screen (Phase 6). A React Context provides this with zero new dependencies.
**Output:** AdminContext.tsx (new), DesktopShell.tsx (wrap in AdminProvider), LoginModal.tsx (admin password check on submit)
</objective>

<execution_context>
@C:/Users/jemar/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/jemar/.config/opencode/get-shit-done/templates/summary.md
@AGENTS.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-favorites-manager/07-CONTEXT.md
@.planning/phases/07-favorites-manager/07-RESEARCH.md
@.planning/phases/07-favorites-manager/07-PATTERNS.md
@.planning/phases/07-favorites-manager/07-UI-SPEC.md

<interfaces>
<!-- Key contracts extracted from existing codebase -->

From components/desktop/DesktopShell.tsx:
- Current return structure (lines 326-434): `<main> ... {ctxMenu} ... {desktopIcons} ... <Taskbar> ... <StartMenu> ... <WindowShell> ... {showShutDownDialog} ... {showLogin && <LoginModal>} ... </main>`
- LoginModal props: `animating`, `onSuccess`, `onCancel`
- Current imports (lines 1-21): React, Next.js router, useWindowManager, useDesktopSounds, Taskbar, StartMenu, DesktopIcon, WindowShell, ContextMenu, programs, LoginModal

From components/desktop/LoginModal.tsx:
- Props interface: `{ onSuccess: () => void; onCancel: () => void; animating?: boolean }`
- State: `const [password, setPassword] = useState("")`
- handleSubmit (lines 27-36): `e.preventDefault()`, `sounds.play("click")`, audio play, `onSuccess()`
- handleCancel (lines 38-41): `sounds.play("close")`, `onCancel()`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AdminContext.tsx</name>
  <files>components/desktop/AdminContext.tsx</files>

  <read_first>
    components/desktop/hooks/useDesktopSounds.ts — existing hook pattern for structure reference
    07-RESEARCH.md — §AdminContext Architecture (lines 191-219) for exact code blueprint
    07-PATTERNS.md — AdminContext pattern map (lines 17-60) for file conventions
  </read_first>

  <action>
    Create `components/desktop/AdminContext.tsx`:

    **Directive:** `"use client"` at line 1.

    **Imports:** `createContext`, `useContext`, `useState`, `type ReactNode` from `"react"`.

    **Interface `AdminContextValue`:**
    - `isAdmin: boolean` — whether the current session has admin privileges
    - `setAdmin: (value: boolean) => void` — setter called by LoginModal on password check

    **Create context:**
    ```typescript
    const AdminContext = createContext<AdminContextValue>({
      isAdmin: false,
      setAdmin: () => {},
    });
    ```

    **Export constant:** `export const ADMIN_SECRET = "jhered";`
    - Exact match, case-sensitive
    - The string "jhered" per RESEARCH.md recommendation (Open Question 1)

    **Export `AdminProvider`:** Component accepting `{ children }: { children: ReactNode }`:
    - `const [isAdmin, setAdmin] = useState(false);`
    - Returns `<AdminContext.Provider value={{ isAdmin, setAdmin }}>{children}</AdminContext.Provider>`

    **Export `useAdmin`:** Hook function returning `useContext(AdminContext)`.

    **File must export exactly:** `AdminProvider` (named), `useAdmin` (named), `ADMIN_SECRET` (named constant).

    **No default export.** The pattern matches React Context convention where Provider is explicitly imported.
  </action>

  <acceptance_criteria>
    - File `components/desktop/AdminContext.tsx` exists with `"use client"` directive
    - `AdminProvider` is a named export wrapping children in `AdminContext.Provider`
    - `useAdmin` is a named export returning `useContext(AdminContext)`
    - `ADMIN_SECRET` is a named export with value `"jhered"`
    - `AdminContext` uses `createContext` with default `{ isAdmin: false, setAdmin: () => {} }`
    - `npm run build` succeeds with zero errors
  </acceptance_criteria>

  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>AdminContext.tsx created with AdminProvider, useAdmin hook, and ADMIN_SECRET constant; build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Wrap DesktopShell content in AdminProvider</name>
  <files>components/desktop/DesktopShell.tsx</files>

  <read_first>
    components/desktop/DesktopShell.tsx — full file, especially imports (lines 1-21), return statement (lines 326-434)
    components/desktop/AdminContext.tsx — the provider being imported (created in Task 1)
    07-RESEARCH.md — §AdminContext Integration in DesktopShell (lines 585-595, 619-633) for exact pattern
    07-PATTERNS.md — DesktopShell modification pattern (lines 64-114) for exact code diff
  </read_first>

  <action>
    Apply two targeted modifications to `components/desktop/DesktopShell.tsx`:

    **Modification 1 — Add import** (after line 19, near existing `import Favorites from "./programs/Favorites";`):
    Insert: `import { AdminProvider } from "./AdminContext";`

    **Modification 2 — Wrap entire return in AdminProvider** (lines 326-434):
    Change the return statement from:
    ```
    return (
      <main ...>
        ...
        {showLogin && (...)}
      </main>
    );
    ```
    To:
    ```
    return (
      <AdminProvider>
        <main ...>
          ...
          {showLogin && (...)}
        </main>
      </AdminProvider>
    );
    ```

    **CRITICAL:** `AdminProvider` must wrap the ENTIRE return — including the `{showLogin && ...}` gate (lines 424-432). Per RESEARCH.md Pitfall 4 (lines 493-497): "The AdminProvider must wrap the ENTIRE return of DesktopShell — including the login gate." This ensures `LoginModal` can access `useAdmin()` context. Without this, the admin state would be lost during the login-to-desktop transition.

    **Do NOT modify** any other part of DesktopShell.tsx. No state changes, no effect changes, no handler changes.
  </action>

  <acceptance_criteria>
    - File `components/desktop/DesktopShell.tsx` imports `AdminProvider` from `"./AdminContext"`
    - The outermost JSX in the `return` is `<AdminProvider>` with `</AdminProvider>` as the closing tag
    - `AdminProvider` wraps ALL content including `showLogin` conditional render, `showBoot` return, and desktop UI
    - No other modifications to DesktopShell.tsx
    - `npm run build` succeeds with zero errors
  </acceptance_criteria>

  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>DesktopShell.tsx wraps entire return in AdminProvider; build succeeds</done>
</task>

<task type="auto">
  <name>Task 3: Add admin password detection to LoginModal</name>
  <files>components/desktop/LoginModal.tsx</files>

  <read_first>
    components/desktop/LoginModal.tsx — full file, especially form handler (lines 27-36), component body (lines 23-91)
    components/desktop/AdminContext.tsx — ADMIN_SECRET and useAdmin hook (created in Task 1)
    07-RESEARCH.md — §LoginModal Admin Detection (lines 598-617) for exact code pattern
    07-PATTERNS.md — LoginModal modification pattern (lines 118-148) for exact code diff
  </read_first>

  <action>
    Apply two targeted modifications to `components/desktop/LoginModal.tsx`:

    **Modification 1 — Add import** (after line 4, after existing `useDesktopSounds` import):
    Insert: `import { ADMIN_SECRET, useAdmin } from "./AdminContext";`

    **Modification 2 — Add hook call and modify handleSubmit** (after line 25 `const sounds = useDesktopSounds();`):
    Insert: `const { setAdmin } = useAdmin();`

    Then modify the existing `handleSubmit` function (lines 27-36). Change from:
    ```typescript
    const handleSubmit = (e: FormEvent) => {
      e.preventDefault();
      sounds.play("click");
      try {
        const loginAudio = new Audio("/audio/windows_98_login.mp3");
        loginAudio.volume = 0.5;
        loginAudio.play().catch(() => {});
      } catch {}
      onSuccess();
    };
    ```
    To:
    ```typescript
    const handleSubmit = (e: FormEvent) => {
      e.preventDefault();
      sounds.play("click");
      setAdmin(password === ADMIN_SECRET); // Match against admin secret — exact match, case-sensitive
      try {
        const loginAudio = new Audio("/audio/windows_98_login.mp3");
        loginAudio.volume = 0.5;
        loginAudio.play().catch(() => {});
      } catch {}
      onSuccess();
    };
    ```

    **Key behavioral requirement:** `setAdmin()` is called BEFORE `onSuccess()` — this ensures admin state is set before the login animation starts and DesktopShell re-renders. Per RESEARCH.md §AdminContext Architecture (lines 222-226): "setAdmin(true) is called inside LoginModal during form submit."

    **Do NOT modify** any other part of LoginModal.tsx. The password field remains accepting any input (per D-06) — admin detection is an additive check, not a restriction.
  </action>

  <acceptance_criteria>
    - File `components/desktop/LoginModal.tsx` imports `ADMIN_SECRET, useAdmin` from `"./AdminContext"`
    - `const { setAdmin } = useAdmin();` hook call present after `const sounds = useDesktopSounds();`
    - `handleSubmit` contains `setAdmin(password === ADMIN_SECRET);` before `onSuccess()`
    - Password field still accepts any input (no validation added — per D-06)
    - `npm run build` succeeds with zero errors
  </acceptance_criteria>

  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>LoginModal.tsx detects admin password and sets admin state; build succeeds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LoginModal password check → AdminContext | Admin secret is hardcoded in client-side JavaScript. No real authentication boundary — cosmetic easter-egg feature toggle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | AdminContext.tsx (ADMIN_SECRET) | Accept | Admin secret is hardcoded in client-side bundle. Per RESEARCH.md: "The admin secret is a cosmetic easter-egg feature, not real auth. Hardcoding it in AdminContext.tsx is acceptable for this use case." ASVS V2 Authentication does not apply. |
| T-07-02 | Tampering | AdminContext state (isAdmin boolean) | Accept | Admin state is a single boolean in React Context (in-memory only). No persisted privileges, no server-side enforcement, no protected resources. Page refresh resets state. |
| T-07-03 | Elevation of Privilege | AdminContext.isAdmin | Accept | No protected resources exist. "Admin" controls only UI visibility on a static portfolio easter-egg page. |
| T-07-SC | Tampering | Package supply chain | N/A | Zero packages installed in this phase. Only existing React primitives and existing project code used. |
</threat_model>

<verification>
- [ ] `npm run build` succeeds with zero errors after all three tasks
- [ ] `npm run lint` passes with no new warnings
- [ ] File `components/desktop/AdminContext.tsx` exists with AdminProvider, useAdmin, ADMIN_SECRET exports
- [ ] File `components/desktop/DesktopShell.tsx` wraps entire return in AdminProvider
- [ ] File `components/desktop/LoginModal.tsx` has admin password detection in handleSubmit
- [ ] Admin state set to true when password "jhered" entered at login
- [ ] Admin state set to false for any other password
- [ ] Admin state accessible via `useAdmin().isAdmin` in any DesktopShell child component
</verification>

<success_criteria>
1. AdminContext.tsx created with AdminProvider (wraps children in context), useAdmin hook (returns isAdmin/setAdmin), ADMIN_SECRET constant (value "jhered")
2. DesktopShell.tsx wraps entire return — including login gate — in AdminProvider
3. LoginModal.tsx calls `setAdmin(password === ADMIN_SECRET)` before `onSuccess()` in handleSubmit
4. Admin state persists through login → boot → desktop transition (no re-mount loss)
5. Page refresh resets admin state (useState(false) initial value)
6. `npm run build` succeeds with zero errors
</success_criteria>

<output>
Create `.planning/phases/07-favorites-manager/07-01-SUMMARY.md` when done
</output>
