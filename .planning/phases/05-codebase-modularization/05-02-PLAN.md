---
id: "05-02"
wave: 2

depends_on: []

autonomous: true
files_modified:
  - "app/hooks/useRevealOnScroll.ts"
  - "app/desktop/hooks/useDesktopSounds.ts"
  - "app/desktop/hooks/useWindowManager.ts"
  - "hooks/useRevealOnScroll.ts"
  - "hooks/useDesktopSounds.ts"
  - "hooks/useWindowManager.ts"
  - "app/page.tsx"
  - "app/components/sections/*.tsx"
  - "app/desktop/components/DesktopShell.tsx"
  - "app/desktop/components/Taskbar.tsx"
  - "app/desktop/components/Window.tsx"
---

# Plan 05-02: Move hooks to root-level hooks/

**Objective:** Move all hooks (`app/hooks/useRevealOnScroll.ts`, `app/desktop/hooks/useDesktopSounds.ts`, `app/desktop/hooks/useWindowManager.ts`) to root-level `hooks/`. Update all import paths.

<tasks>
<task id="01" type="execute">
<read_first>
- ls app/hooks/
- ls app/desktop/hooks/
</read_first>
<action>
Create root-level `hooks/` directory. Move files:
- `app/hooks/useRevealOnScroll.ts` → `hooks/useRevealOnScroll.ts`
- `app/desktop/hooks/useDesktopSounds.ts` → `hooks/useDesktopSounds.ts`
- `app/desktop/hooks/useWindowManager.ts` → `hooks/useWindowManager.ts`

Delete old locations after successful move.
</action>
<acceptance_criteria>
- [ ] `hooks/useRevealOnScroll.ts` exists at root
- [ ] `hooks/useDesktopSounds.ts` exists at root
- [ ] `hooks/useWindowManager.ts` exists at root
- [ ] `app/hooks/` directory no longer exists (or is empty)
- [ ] `app/desktop/hooks/` directory no longer exists (or is empty)
</acceptance_criteria>
</task>

<task id="02" type="execute">
<read_first>
- Use `grep -r` to find all imports from `@/app/hooks/` and relative imports to `../hooks/` in desktop files
</read_first>
<action>
Update all import paths:
- `@/app/hooks/...` → `@/hooks/...`
- In `DesktopShell.tsx`: `../hooks/useDesktopSounds` → `@/hooks/useDesktopSounds`
- In `DesktopShell.tsx`: `../hooks/useWindowManager` → `@/hooks/useWindowManager`
- In `Taskbar.tsx`: `../hooks/useWindowManager` → `@/hooks/useWindowManager`
- In `Window.tsx`: `../hooks/useWindowManager` → `@/hooks/useWindowManager`
</action>
<acceptance_criteria>
- [ ] No remaining `from "@/app/hooks/` imports in any file
- [ ] No remaining relative `../hooks/` imports from `app/` subdirectories
- [ ] All hook consumers import from `@/hooks/...`
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `npm run build` succeeds
2. Desktop icons, windows, and taskbar work correctly (useWindowManager)
3. Scroll reveal animations work (useRevealOnScroll)
4. Sound effects play (useDesktopSounds)
</verification>

<must_haves>
- Root-level `hooks/` directory with all 3 hook files
- All import paths updated
- `npm run build` passes
</must_haves>
