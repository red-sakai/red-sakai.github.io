---
id: "05-05"
wave: 5

depends_on: ["05-01", "05-02", "05-03", "05-04"]
autonomous: true
files_modified:
  - "app/components/" (delete empty tree)
  - "app/hooks/" (delete if empty)
  - "app/types/" (delete if empty)
  - "app/data/" (delete if empty)
  - "app/lib/" (delete if empty)
  - "app/actions/" (delete)
  - "app/components/desktop/" (empty — delete)
  - "app/desktop/components/" (delete if empty)
  - "app/desktop/data/" (delete if empty)
  - "app/desktop/hooks/" (delete if empty)
---

# Plan 05-05: Cleanup empty directories and verify build

**Objective:** Remove all empty directories left behind by the migration. Verify that `npm run build` passes cleanly with the final structure.

<tasks>
<task id="01" type="execute">
<read_first>
- Check which directories under `app/` are now empty using `find app/ -type d -empty`
</read_first>
<action>
Remove empty directories that are no longer needed:
- `app/components/` (entire tree — sections, ui, providers, desktop)
- `app/hooks/`
- `app/types/`
- `app/lib/` (if empty)
- `app/actions/` (was already empty)
- `app/desktop/data/`
- `app/desktop/hooks/`
- `app/components/` at root level if it was an empty leftover

Use `git rm -r` or `rmdir` for each empty directory. Only remove directories that are confirmed empty.
</action>
<acceptance_criteria>
- [ ] `app/components/` no longer exists
- [ ] `app/hooks/` no longer exists
- [ ] `app/types/` no longer exists
- [ ] `app/data/` no longer exists
- [ ] `app/actions/` no longer exists
- [ ] `app/lib/` no longer exists (if it was moved/emptied)
- [ ] `app/desktop/data/` no longer exists
- [ ] `app/desktop/hooks/` no longer exists
- [ ] No empty leftover directories from the migration
</acceptance_criteria>
</task>

<task id="02" type="execute">
<read_first>
- Run `npm run build` to check for errors
</read_first>
<action>
Run `npm run build` and verify it passes with zero errors. If any import paths are broken, fix them.
</action>
<acceptance_criteria>
- [ ] `npm run build` succeeds with zero errors and zero warnings
- [ ] All routes compile successfully
</acceptance_criteria>
</task>

<task id="03" type="execute">
<read_first>
- git status (check for untracked files that should be gitignored)
</read_first>
<action>
Run `git status` to verify the working tree is clean after all moves. Verify no duplicate files exist (e.g., a file at both `app/lib/utils.ts` and `lib/utils.ts`).
</action>
<acceptance_criteria>
- [ ] `git status` shows only the expected moved/deleted files
- [ ] No duplicate files between `app/` and root-level directories
- [ ] All moved files are tracked in their new locations
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `git status` shows a clean picture of the refactored structure
3. All routes compile — no missing imports
</verification>

<must_haves>
- All empty `app/` subdirectories removed
- `npm run build` passes
- Working tree is clean (no stale files in old locations)
</must_haves>
