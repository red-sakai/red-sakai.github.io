---
id: "05-01"
wave: 1
depends_on: []
autonomous: true
files_modified:
  - "app/lib/utils.ts"
  - "app/types/model-viewer.d.ts"
  - "app/data/wallpapers-generated.ts"
  - "app/data/*.json"
  - "scripts/generate-wallpapers.js"
  - "lib/utils.ts"
  - "types/model-viewer.d.ts"
  - "data/wallpapers-generated.ts"
  - "data/*.json"
  - "app/desktop/components/programs/ControlPanel.tsx"
---

# Plan 05-01: Move lib + types + data to project root

**Objective:** Move `app/lib/utils.ts`, `app/types/model-viewer.d.ts`, and all `app/data/` files to root-level directories. Update the prebuild script output path and all imports that reference these files.

<tasks>
<task id="01" type="execute">
<read_first>
- tailwind.config.ts (check how @/ resolves for CSS)
</read_first>
<action>
Create root-level directories `lib/`, `types/`, `data/` if they don't exist. Move files:
- `app/lib/utils.ts` → `lib/utils.ts` (note: lib/ already exists at root — merge)
- `app/types/model-viewer.d.ts` → `types/model-viewer.d.ts`
- `app/data/certifications.json` → `data/certifications.json`
- `app/data/education.json` → `data/education.json`
- `app/data/experience.json` → `data/experience.json`
- `app/data/favorites.json` → `data/favorites.json`
- `app/data/projects.json` → `data/projects.json`
- `app/data/wallpapers-generated.ts` → `data/wallpapers-generated.ts`

Delete old locations after successful move.
</action>
<acceptance_criteria>
- [ ] `lib/utils.ts` exists at root level
- [ ] `types/model-viewer.d.ts` exists at root level
- [ ] All 5 JSON files exist at `data/*.json` root level
- [ ] `data/wallpapers-generated.ts` exists at root level
- [ ] Old `app/lib/utils.ts`, `app/types/`, `app/data/*` no longer exist
</acceptance_criteria>
</task>

<task id="02" type="execute">
<read_first>
- scripts/generate-wallpapers.js (current output path)
</read_first>
<action>
Update `scripts/generate-wallpapers.js` to output to `data/wallpapers-generated.ts` (root level) instead of `app/data/wallpapers-generated.ts`.
</action>
<acceptance_criteria>
- [ ] `scripts/generate-wallpapers.js` writes to `app/data/../data/wallpapers-generated.ts` → changed to `data/wallpapers-generated.ts`
- [ ] Running `node scripts/generate-wallpapers.js` produces the file at root-level `data/wallpapers-generated.ts`
</acceptance_criteria>
</task>

<task id="03" type="execute">
<read_first>
- app/desktop/components/programs/ControlPanel.tsx (imports WALLPAPER_PRESETS)
- Any file with `from "@/app/data/` or `from "@/app/types/"` or `from "@/app/lib/"` imports
</read_first>
<action>
Update all import paths (use `grep -r` to find all occurrences):
- `@/app/data/...` → `@/data/...`
- `@/app/types/...` → `@/types/...`
- `@/app/lib/...` → `@/lib/...`
</action>
<acceptance_criteria>
- [ ] No remaining `from "@/app/data/` imports in any `.ts` or `.tsx` file
- [ ] No remaining `from "@/app/types/` imports
- [ ] No remaining `from "@/app/lib/` imports
- [ ] `ControlPanel.tsx` imports from `@/data/wallpapers-generated`
</acceptance_criteria>
</task>

<task id="04" type="execute">
<read_first>
- app/data/favorites.json (referenced via relative path in Favorites.tsx)
- app/desktop/components/programs/Favorites.tsx (has `../../../data/favorites.json`)
</read_first>
<action>
Update `Favorites.tsx` relative import from `../../../data/favorites.json` to `@/data/favorites.json` (since data/ moved to root).
</action>
<acceptance_criteria>
- [ ] `Favorites.tsx` imports from `@/data/favorites.json`
- [ ] No remaining relative imports to `app/data/` or `data/` that break
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. All wallpaper presets load in Control Panel
3. All JSON-driven content (education, experience, projects, certifications) renders correctly
4. TypeScript types (`model-viewer.d.ts`) resolve correctly
</verification>

<must_haves>
- Root-level `lib/`, `types/`, `data/` directories populated
- Prebuild script outputs to correct root-level path
- All `@/app/data/`, `@/app/types/`, `@/app/lib/` imports updated to root-level paths
- `npm run build` passes
</must_haves>
